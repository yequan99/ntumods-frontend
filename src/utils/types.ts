export type ModuleMetaData = {
    moduleCode: string,
    title: string,
    description: string,
    credits: number
}

export type FilterData = {
    moduleCode: string,
    title: string,
    faculty: string,
    semester: number,
    moduleType: string
}