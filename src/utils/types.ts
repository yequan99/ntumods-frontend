export type ModuleMetaData = {
    moduleCode: string,
    title: string,
    description: string,
    credits: number
}

export type FilterData = {
    query: string,
    faculty: string,
    semester: string,
    moduleType: string
}